
    "originalProgramId": "T2",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_804",
    "originalProgramId": "T2",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_805",
    "originalProgramId": "T3",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_806",
    "originalProgramId": "T3",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     } }"
  },
  {
    "id": "pair_807",
    "originalProgramId": "T2",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_808",
    "originalProgramId": "T1",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        let i = 0; while(i < items.length) { {\n          sum += items[i].price * items[i].quantity;\n        }\n        i++; }\nreturn sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_809",
    "originalProgramId": "T3",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function computeResult(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_810",
    "originalProgramId": "T2",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_811",
    "originalProgramId": "T3",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function sortAndFilter(arr, threshold) { dep2();\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_812",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_813",
    "originalProgramId": "T3",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_814",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_815",
    "originalProgramId": "T3",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_816",
    "originalProgramId": "T3",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_817",
    "originalProgramId": "T2",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \n\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_818",
    "originalProgramId": "T1",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     }"
  },
  {
    "id": "pair_819",
    "originalProgramId": "T3",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     }"
  },
  {
    "id": "pair_820",
    "originalProgramId": "T3",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_821",
    "originalProgramId": "T2",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_822",
    "originalProgramId": "T3",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_823",
    "originalProgramId": "T3",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_824",
    "originalProgramId": "T1",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(products, taxRate) {\n        let totalAmount = 0;\n        for (let i = 0; i < products.length; i++) {\n          totalAmount += products[i].price * products[i].quantity;\n        }\n        return totalAmount + (totalAmount * taxRate);\n      }\n    "
  },
  {
    "id": "pair_825",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_826",
    "originalProgramId": "T1",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function calculateTotal(items, taxRate) { dep2();\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_827",
    "originalProgramId": "T1",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += item.price * item.quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_828",
    "originalProgramId": "T2",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function validateUser(user) { dep2();\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_829",
    "originalProgramId": "T2",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_830",
    "originalProgramId": "T2",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_831",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_832",
    "originalProgramId": "T2",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_833",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_834",
    "originalProgramId": "T2",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     }"
  },
  {
    "id": "pair_835",
    "originalProgramId": "T1",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function calculateTotal(items, taxRate) { dep2();\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_836",
    "originalProgramId": "T1",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function computeResult(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_837",
    "originalProgramId": "T2",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_838",
    "originalProgramId": "T2",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_839",
    "originalProgramId": "T1",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        let i = 0; while(i < items.length) { {\n          sum += items[i].price * items[i].quantity;\n        }\n        i++; }\nreturn sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_840",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_841",
    "originalProgramId": "T3",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     }"
  },
  {
    "id": "pair_842",
    "originalProgramId": "T1",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_843",
    "originalProgramId": "T1",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0; function getZero() { return 0; } sum = getZero();\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_844",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_845",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_846",
    "originalProgramId": "T1",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function computeResult(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_847",
    "originalProgramId": "T1",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     } }"
  },
  {
    "id": "pair_848",
    "originalProgramId": "T2",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_849",
    "originalProgramId": "T2",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_850",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_851",
    "originalProgramId": "T3",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_852",
    "originalProgramId": "T1",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        let i = 0; while(i < items.length) { {\n          sum += items[i].price * items[i].quantity;\n        }\n        i++; }\nreturn sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_853",
    "originalProgramId": "T3",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \n\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_854",
    "originalProgramId": "T3",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_855",
    "originalProgramId": "T2",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function computeResult(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_856",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_857",
    "originalProgramId": "T2",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     }"
  },
  {
    "id": "pair_858",
    "originalProgramId": "T2",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function validateUser(user) { dep2();\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_859",
    "originalProgramId": "T2",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_860",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_861",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_862",
    "originalProgramId": "T3",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_863",
    "originalProgramId": "T3",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \n\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_864",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_865",
    "originalProgramId": "T1",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_866",
    "originalProgramId": "T3",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function sortAndFilter(arr, threshold) { dep2();\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_867",
    "originalProgramId": "T1",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     } }"
  },
  {
    "id": "pair_868",
    "originalProgramId": "T1",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_869",
    "originalProgramId": "T1",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_870",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_871",
    "originalProgramId": "T3",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_872",
    "originalProgramId": "T1",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += item.price * item.quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_873",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_874",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_875",
    "originalProgramId": "T2",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function validateUser(user) { dep2();\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_876",
    "originalProgramId": "T3",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function sortAndFilter(arr, threshold) { dep2();\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_877",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_878",
    "originalProgramId": "T3",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_879",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_880",
    "originalProgramId": "T2",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     }"
  },
  {
    "id": "pair_881",
    "originalProgramId": "T3",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_882",
    "originalProgramId": "T2",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_883",
    "originalProgramId": "T3",
    "transformationChain": [
      "O"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_884",
    "originalProgramId": "T2",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_885",
    "originalProgramId": "T1",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0; function getZero() { return 0; } sum = getZero();\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_886",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_887",
    "originalProgramId": "T2",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_888",
    "originalProgramId": "T1",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += item.price * item.quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_889",
    "originalProgramId": "T1",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "(function() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     })();"
  },
  {
    "id": "pair_890",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_891",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_892",
    "originalProgramId": "T2",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_893",
    "originalProgramId": "T1",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     }"
  },
  {
    "id": "pair_894",
    "originalProgramId": "T3",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_895",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_896",
    "originalProgramId": "T3",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function computeResult(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_897",
    "originalProgramId": "T1",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_898",
    "originalProgramId": "T3",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_899",
    "originalProgramId": "T3",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_900",
    "originalProgramId": "T3",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_901",
    "originalProgramId": "T1",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function calculateTotal(items, taxRate) { dep2();\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_902",
    "originalProgramId": "T1",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        let i = 0; while(i < items.length) { {\n          sum += items[i].price * items[i].quantity;\n        }\n        i++; }\nreturn sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_903",
    "originalProgramId": "T2",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function validateUser(user) { dep2();\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_904",
    "originalProgramId": "T2",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_905",
    "originalProgramId": "T1",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function computeResult(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_906",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_907",
    "originalProgramId": "T1",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += getLineItemTotal(items[i]);\n        }\n        return sum + (sum * taxRate);\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_908",
    "originalProgramId": "T3",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_909",
    "originalProgramId": "T3",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_910",
    "originalProgramId": "T2",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_911",
    "originalProgramId": "T3",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_912",
    "originalProgramId": "T3",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_913",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_914",
    "originalProgramId": "T2",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function computeResult(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_915",
    "originalProgramId": "T2",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \n\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_916",
    "originalProgramId": "T1",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0; function getZero() { return 0; } sum = getZero();\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_917",
    "originalProgramId": "T2",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \n\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_918",
    "originalProgramId": "T2",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_919",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_920",
    "originalProgramId": "T1",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        let i = 0; while(i < items.length) { {\n          sum += items[i].price * items[i].quantity;\n        }\n        i++; }\nreturn sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_921",
    "originalProgramId": "T2",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_922",
    "originalProgramId": "T1",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function calculateTotal(items, taxRate) { dep2();\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_923",
    "originalProgramId": "T2",
    "transformationChain": [
      "O"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_924",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_925",
    "originalProgramId": "T2",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_926",
    "originalProgramId": "T1",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_927",
    "originalProgramId": "T2",
    "transformationChain": [
      "O"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_928",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_929",
    "originalProgramId": "T3",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function sortAndFilter(arr, threshold) { dep2();\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_930",
    "originalProgramId": "T3",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \n\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_931",
    "originalProgramId": "T2",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     }"
  },
  {
    "id": "pair_932",
    "originalProgramId": "T2",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     }"
  },
  {
    "id": "pair_933",
    "originalProgramId": "T3",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_934",
    "originalProgramId": "T3",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     } }"
  },
  {
    "id": "pair_935",
    "originalProgramId": "T3",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function sortAndFilter(arr, threshold) { dep2();\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_936",
    "originalProgramId": "T2",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     }"
  },
  {
    "id": "pair_937",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_938",
    "originalProgramId": "T3",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_939",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_940",
    "originalProgramId": "T1",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     }"
  },
  {
    "id": "pair_941",
    "originalProgramId": "T1",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += getLineItemTotal(items[i]);\n        }\n        return sum + (sum * taxRate);\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_942",
    "originalProgramId": "T3",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_943",
    "originalProgramId": "T2",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function validateUser(user) { dep2();\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_944",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_945",
    "originalProgramId": "T2",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_946",
    "originalProgramId": "T1",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_947",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_948",
    "originalProgramId": "T1",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(products, taxRate) {\n        let totalAmount = 0;\n        for (let i = 0; i < products.length; i++) {\n          totalAmount += products[i].price * products[i].quantity;\n        }\n        return totalAmount + (totalAmount * taxRate);\n      }\n    "
  },
  {
    "id": "pair_949",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_950",
    "originalProgramId": "T2",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_951",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_952",
    "originalProgramId": "T3",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_953",
    "originalProgramId": "T1",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_954",
    "originalProgramId": "T3",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     } }"
  },
  {
    "id": "pair_955",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_956",
    "originalProgramId": "T3",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     }"
  },
  {
    "id": "pair_957",
    "originalProgramId": "T1",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_958",
    "originalProgramId": "T1",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     } }"
  },
  {
    "id": "pair_959",
    "originalProgramId": "T1",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n     } }"
  },
  {
    "id": "pair_960",
    "originalProgramId": "T3",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_961",
    "originalProgramId": "T1",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_962",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_963",
    "originalProgramId": "T2",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_964",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_965",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_966",
    "originalProgramId": "T3",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_967",
    "originalProgramId": "T3",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_968",
    "originalProgramId": "T3",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_969",
    "originalProgramId": "T3",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_970",
    "originalProgramId": "T3",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_971",
    "originalProgramId": "T1",
    "transformationChain": [
      "N"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += item.price * item.quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_972",
    "originalProgramId": "T2",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \n\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_973",
    "originalProgramId": "T1",
    "transformationChain": [
      "O"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum * 2;\n      }\n    "
  },
  {
    "id": "pair_974",
    "originalProgramId": "T1",
    "transformationChain": [
      "M"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_975",
    "originalProgramId": "T2",
    "transformationChain": [
      "C"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function wrapper() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     }"
  },
  {
    "id": "pair_976",
    "originalProgramId": "T1",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_977",
    "originalProgramId": "T2",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "(function() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     })();"
  },
  {
    "id": "pair_978",
    "originalProgramId": "T2",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_979",
    "originalProgramId": "T3",
    "transformationChain": [
      "A"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_980",
    "originalProgramId": "T1",
    "transformationChain": [
      "I"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \nfunction extra() { return 1; }"
  },
  {
    "id": "pair_981",
    "originalProgramId": "T2",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function validateUser(user) { dep2();\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_982",
    "originalProgramId": "T2",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_983",
    "originalProgramId": "T1",
    "transformationChain": [
      "J"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        let i = 0; while(i < items.length) { {\n          sum += items[i].price * items[i].quantity;\n        }\n        i++; }\nreturn sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_984",
    "originalProgramId": "T2",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \n\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_985",
    "originalProgramId": "T1",
    "transformationChain": [
      "P"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \nfunction noop() {}\nfunction identity(x) { return x; }"
  },
  {
    "id": "pair_986",
    "originalProgramId": "T2",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_987",
    "originalProgramId": "T2",
    "transformationChain": [
      "K"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_988",
    "originalProgramId": "T2",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function computeResult(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_989",
    "originalProgramId": "T1",
    "transformationChain": [
      "Q"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    \n\n      function calculateTotalCopy(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_990",
    "originalProgramId": "T2",
    "transformationChain": [
      "L"
    ],
    "expectedRelationship": "PARTIAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    \nfunction getLineItemTotal(item) { return item.price * item.quantity; }"
  },
  {
    "id": "pair_991",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_992",
    "originalProgramId": "T3",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_993",
    "originalProgramId": "T3",
    "transformationChain": [
      "G"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "const dep1 = () => 1; const dep2 = () => dep1(); \n\n      function sortAndFilter(arr, threshold) { dep2();\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_994",
    "originalProgramId": "T3",
    "transformationChain": [
      "E"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "(function() { \n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n     })();"
  },
  {
    "id": "pair_995",
    "originalProgramId": "T2",
    "transformationChain": [
      "F"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) { const unusedVar = 42; if (false) { console.log(\"dead\"); }\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_996",
    "originalProgramId": "T2",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_997",
    "originalProgramId": "T2",
    "transformationChain": [
      "D"
    ],
    "expectedRelationship": "STRUCTURAL_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "function outer() { function inner() { \n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n     } }"
  },
  {
    "id": "pair_998",
    "originalProgramId": "T2",
    "transformationChain": [
      "B"
    ],
    "expectedRelationship": "EXACT_MATCH",
    "sourceVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    ",
    "targetVariant": "\n      function computeResult(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_999",
    "originalProgramId": "T1",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0; function getZero() { return 0; } sum = getZero();\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    "
  },
  {
    "id": "pair_1000",
    "originalProgramId": "T3",
    "transformationChain": [
      "H"
    ],
    "expectedRelationship": "EVOLVED_MATCH",
    "sourceVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    ",
    "targetVariant": "\n      function sortAndFilter(arr, threshold) {\n        const filtered = [];\n        for (const val of arr) {\n          if (val > threshold) filtered.push(val);\n        }\n        return filtered.sort((a, b) => a - b);\n      }\n    "
  },
  {
    "id": "pair_1001",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1002",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1003",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1004",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1005",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1006",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1007",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1008",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1009",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1010",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1011",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1012",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1013",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1014",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1015",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1016",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1017",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1018",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1019",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1020",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1021",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1022",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1023",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1024",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1025",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1026",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1027",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1028",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1029",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1030",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1031",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1032",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1033",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1034",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1035",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1036",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1037",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1038",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1039",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1040",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1041",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1042",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1043",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1044",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1045",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1046",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1047",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1048",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1049",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  },
  {
    "id": "pair_1050",
    "originalProgramId": "MIX",
    "transformationChain": [
      "NEGATIVE"
    ],
    "expectedRelationship": "NO_MATCH",
    "sourceVariant": "\n      function calculateTotal(items, taxRate) {\n        let sum = 0;\n        for (let i = 0; i < items.length; i++) {\n          sum += items[i].price * items[i].quantity;\n        }\n        return sum + (sum * taxRate);\n      }\n    ",
    "targetVariant": "\n      function validateUser(user) {\n        if (!user) return false;\n        if (!user.email || !user.email.includes('@')) return false;\n        if (user.age < 18) return false;\n        return true;\n      }\n    "
  }
]